
{
  "version": 2,
  "framework": "vite",
  "buildCommand": "echo 'Static site - no build needed' && exit 0",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
